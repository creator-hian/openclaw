FROM openclaw:local
USER root
RUN apt-get update -qq \
    && apt-get install -y -qq --no-install-recommends python3-yaml \
    && rm -rf /var/lib/apt/lists/*
RUN npm install -g clawhub
RUN mkdir -p /home/node/.config/clawhub && chown -R 1026:100 /home/node/.config
USER node
